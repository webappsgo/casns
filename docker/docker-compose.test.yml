---
services:
  casns:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - "53:53/tcp"
      - "53:53/udp"
    volumes:
      - './volumes/config:/config:z'
      - './volumes/data:/data:z'
    environment:
      - MODE=development
      - DEBUG=true
